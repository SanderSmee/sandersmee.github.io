header.header-section
    .intro-header(style='background-image: url(' + (config.category.header[page.category] || theme.index_cover) + ');')
        .container
            .row.justify-content-center
                .col-lg-8.col-12
                    .page-heading
                        h1!= page.category
                        hr.small
                        span.page-subheading!="All posts in category '" + page.category + "'"
        span.img-desc
